name: Release

on:
  workflow_dispatch:
    inputs:
      release_type:
        description: Select the version bump to publish
        required: true
        default: patch
        type: choice
        options:
          - patch
          - minor
          - major
          - prepatch
          - preminor
          - premajor
          - prerelease
  pull_request:
    types:
      - closed
    branches:
      - master

permissions:
  contents: write
  pull-requests: write
  id-token: write

jobs:
  open-release-pr:
    if: github.event_name == 'workflow_dispatch' && github.ref_name == 'master'
    runs-on: ubuntu-latest
    concurrency:
      group: release-open-${{ github.ref }}
      cancel-in-progress: false

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Setup Vite+
        uses: voidzero-dev/setup-vp@250f29ce396baf5e8f24498e17c0dfdebabc26eb # v1
        with:
          node-version: "lts"
          cache: true

      - name: Install dependencies
        run: vp install

      - name: Run checks
        run: vp check

      - name: Run tests
        run: vp test

      - name: Configure Git user
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Bump version with bumpp
        run: |
          vp exec bumpp \
            --release "${{ inputs.release_type }}" \
            --all \
            --yes \
            --no-push \
            --no-tag \
            --execute "vp install" \
            --commit "Bump version to v%s"

      - name: Read version
        id: version
        shell: bash
        run: |
          version=$(node -e "console.log(JSON.parse(require('node:fs').readFileSync('package.json', 'utf8')).version)")
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Ensure release automation token is configured
        env:
          RELEASE_PAT: ${{ secrets.RELEASE_PAT }}
        shell: bash
        run: |
          if [ -z "$RELEASE_PAT" ]; then
            echo "Missing RELEASE_PAT secret. Add a fine-grained token with Contents and Pull requests write access."
            exit 1
          fi

      - name: Push release branch
        id: push_branch
        env:
          RELEASE_PAT: ${{ secrets.RELEASE_PAT }}
        shell: bash
        run: |
          branch="release/v${{ steps.version.outputs.version }}"
          git remote set-url origin "https://x-access-token:${RELEASE_PAT}@github.com/${{ github.repository }}.git"

          if git ls-remote --exit-code --heads origin "$branch" >/dev/null 2>&1; then
            echo "Branch $branch already exists. Please delete it or choose a different version."
            exit 1
          fi

          git switch -c "$branch"
          git push --set-upstream origin "$branch"
          echo "branch=$branch" >> "$GITHUB_OUTPUT"

      - name: Create release pull request
        env:
          GH_TOKEN: ${{ secrets.RELEASE_PAT }}
        run: |
          gh pr create \
            --base master \
            --head "${{ steps.push_branch.outputs.branch }}" \
            --title "Release v${{ steps.version.outputs.version }}" \
            --body "Automated release PR created by GitHub Actions."

  publish:
    if: >
      github.event_name == 'pull_request' &&
      github.event.pull_request.merged == true &&
      github.event.pull_request.base.ref == 'master' &&
      startsWith(github.event.pull_request.head.ref, 'release/')
    runs-on: ubuntu-latest
    concurrency:
      group: release-publish-${{ github.ref }}
      cancel-in-progress: false

    steps:
      - name: Checkout merged master
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: master
          fetch-depth: 0

      - name: Setup Vite+
        uses: voidzero-dev/setup-vp@250f29ce396baf5e8f24498e17c0dfdebabc26eb # v1
        with:
          node-version: "lts"
          cache: true

      - name: Install dependencies
        run: vp install

      - name: Run checks
        run: vp check

      - name: Run tests
        run: vp test

      - name: Read version
        id: version
        shell: bash
        run: |
          version=$(node -e "console.log(JSON.parse(require('node:fs').readFileSync('package.json', 'utf8')).version)")
          echo "tag=v$version" >> "$GITHUB_OUTPUT"
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Validate version matches release branch
        shell: bash
        run: |
          expected_branch="release/v${{ steps.version.outputs.version }}"
          actual_branch="${{ github.event.pull_request.head.ref }}"
          if [[ "$expected_branch" != "$actual_branch" ]]; then
            echo "Version mismatch: package.json has v${{ steps.version.outputs.version }}, but PR branch is $actual_branch"
            exit 1
          fi

      - name: Configure Git user
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Create and push tag
        id: create_tag
        shell: bash
        run: |
          if git rev-parse "${{ steps.version.outputs.tag }}" >/dev/null 2>&1; then
            echo "Tag already exists: ${{ steps.version.outputs.tag }}, skipping publish and release."
            echo "created=false" >> "$GITHUB_OUTPUT"
          else
            git tag "${{ steps.version.outputs.tag }}"
            git push origin "${{ steps.version.outputs.tag }}"
            echo "created=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish to npm
        if: steps.create_tag.outputs.created == 'true'
        run: vp pm publish --no-git-checks --access public -- --provenance

      - name: Generate release notes with git-cliff
        if: steps.create_tag.outputs.created == 'true'
        uses: orhun/git-cliff-action@a9a95522b26fe6403f7bb24031f21fb573d0f5ff # v4.9.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          config: cliff.toml
          args: --latest --strip all --output RELEASE_NOTES.md

      - name: Create GitHub release
        if: steps.create_tag.outputs.created == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release create "${{ steps.version.outputs.tag }}" --title "Release ${{ steps.version.outputs.tag }}" --notes-file RELEASE_NOTES.md
